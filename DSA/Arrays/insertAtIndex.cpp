#include<bits/stdc++.h>
using namespace std;
void insertAtIndex(vector<int>&arr,int index,int value){
	int n = arr.size();
	for(int i = index ; i < n; i++){
		arr[]
	}
}
int main(){
	int n;
	cin >> n;
	vector<int>arr(n);
	for(int i = 0 ; i < n ; i++){
		cin >> arr[i];
	}
	int index;
	cin >> index;
	int val;
	cin >> val;
	insertAtIndex(arr,index,val);
	for(auto i : arr){
		cout << i << " ";
	}
}